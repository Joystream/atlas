import styled from '@emotion/styled'

import { WidgetTile } from '@/components/WidgetTile'
import { cVar } from '@/styles'

export const GlassDetailsWidget = styled(WidgetTile)`
  background: #bcd5fa14;
  border-radius: ${cVar('radiusLarge')};
`
