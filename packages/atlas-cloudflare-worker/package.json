{
  "name": "@joystream/atlas-cloudflare-worker",
  "license": "GPL-3.0",
  "version": "1.0.0",
  "description": "Atlas Cloudflare worker",
  "main": "dist/worker.js",
  "scripts": {
    "build": "webpack",
    "format": "prettier --write  '*.{json,js}' 'src/**/*.{js,ts}' 'test/**/*.{js,ts}'",
    "lint": "eslint --max-warnings=0 src && prettier --check '*.{json,js}' 'src/**/*.{js,ts}'"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^3.3.1",
    "ts-loader": "^9.2.6",
    "webpack": "^5.66.0",
    "webpack-cli": "^4.9.1"
  }
}
